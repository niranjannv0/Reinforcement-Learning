# requirements.txt
# --------------------------------------------------------------------------
# python/q_learning_gridworld.py uses matplotlib for its interactive GUI
# (click-to-step buttons, colored Q-value grid) and for saving PNG snapshots
# in --headless mode.
#
#   Requires: Python 3.8+
#   Install:  pip install -r requirements.txt
#   Run:      python python/q_learning_gridworld.py            (interactive GUI)
#             python python/q_learning_gridworld.py --headless  (no window, saves a PNG)

matplotlib>=3.5

# --------------------------------------------------------------------------
# Placeholders for future roadmap items (see README) -- uncomment as they land:
#
#   numpy>=1.24            # Deep Q-Networks (planned)
#   torch>=2.2              # Deep Q-Networks / function approximation (planned)
